<div class="bg-white p-6 rounded-3xl border border-slate-200 shadow-sm">
    <div class="w-12 h-12 bg-{{ $color }}-50 text-{{ $color }}-600 rounded-2xl flex items-center justify-center mb-4">
        <i class="fa-solid {{ $icon }}"></i>
    </div>
    <p class="text-slate-500 text-sm font-semibold uppercase tracking-wider">{{ $title }}</p>
    <p class="text-2xl font-extrabold mt-1">{{ $value }}</p>
</div>
